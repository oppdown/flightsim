# Flight Lab Public Site

This is the public presentation layer for the standalone Flight Lab simulator.
It embeds the current simulator build and presents the project changelog.

The simulator source of truth remains the repository root at
`../flight-simulator.html`; refresh `public/flight-simulator.html` when a new
simulator iteration is ready for publication.
